// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model student {
  student_id Int        @id @default(autoincrement())
  userName   String
  password   String
  education  String
  age        Int
  wallet     Int
  image      String
  email      String     @unique
  DM         DM[]
  review     review[]
  schedule   schedule[]
}

model teacher {
  teacher_id     Int            @id @default(autoincrement())
  numberOfaRtes   Int           
  sumOfRates      Int           
  email          String         @unique
  userName       String
  password       String
  education      String
  description    String
  age            Int
  wallet         Int
  image          String
  Overall_rating String        @default("0")
  DM             DM[]
  free_course    free_course[]
  post           post[]
  review         review[]
  schedule       schedule[]
  weekDay        weekDay[]
  sessions       sessions[]
  price           Int
}

model weekDay {
  weekDay_id Int        @id @default(autoincrement())
  teacher_id Int
  monday     Boolean    @default(false)
  tuesday    Boolean    @default(false)
  wednesday  Boolean    @default(false)
  thursday   Boolean    @default(false)
  friday     Boolean    @default(false)
  saturday   Boolean    @default(false)
  sunday     Boolean    @default(false)
  teacher    teacher    @relation(fields: [teacher_id], references: [teacher_id])
  schedule   schedule[]
}

model sessions {
  sessions_id Int        @id @default(autoincrement())
  teacher_id  Int
  one         Boolean    @default(false)
  two         Boolean    @default(false)
  three       Boolean    @default(false)
  four        Boolean    @default(false)
  five        Boolean    @default(false)
  six         Boolean    @default(false)
  teacher     teacher    @relation(fields: [teacher_id], references: [teacher_id])
  schedule    schedule[]
}

model post {
  post_id   Int     @id @default(autoincrement())
  author_id Int
  Image     String
  status    String  @default("pending")
  body      String
  title     String
  author    teacher @relation(fields: [author_id], references: [teacher_id])
}

model review {
  review_id           Int     @id @default(autoincrement())
  overallRating       Int
  ratesNumber         Int
  student_id             Int
  teacher_id          Int
  comments            String  @default("")
  student          student @relation(fields: [student_id], references: [student_id])
  teacher          teacher  @relation(fields: [teacher_id], references: [teacher_id])
}

model schedule {
  scheduel_id      Int      @id @default(autoincrement())
  student          Int
  teacher          Int
  day              Int
  session          Int
  studentCallId    Int      @unique
  student_id       student  @relation(fields: [student], references: [student_id])
  teacher_id       teacher  @relation(fields: [teacher], references: [teacher_id])
  day_relation     weekDay  @relation(fields: [day], references: [weekDay_id])
  session_relation sessions @relation(fields: [session], references: [sessions_id])
}

model attachement {
  attachement_id Int           @id @default(autoincrement())
  Type           String
  attachement    String
  body           String
  free_course    free_course[]
}

model free_course {
  freeCourse_id Int         @id @default(autoincrement())
  teacher       Int
  title         String
  category      String
  Status        String      @default("pending")
  document      Int
  image         String
  attachement   attachement @relation(fields: [document], references: [attachement_id])
  teacher_id    teacher     @relation(fields: [teacher], references: [teacher_id])
}

model DM {
  DM_id            Int     @id @default(autoincrement())
  student          Int
  teacher          Int
  teacher_messages String
  student_messages String
  student_id       student @relation(fields: [student], references: [student_id])
  teacher_id       teacher @relation(fields: [teacher], references: [teacher_id])
}